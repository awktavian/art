aiofiles>=23.2.1
aiohttp>=3.8.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
beautifulsoup4>=4.12.0  # For web scraping (Research agent, web search API)
celery>=5.3.0  # Distributed task queue
composio-core==0.7.21  # Latest resolvable on PyPI as of 2025-07; V3 SDK
coremltools>=6.0; platform_system=="Darwin"  # For Core ML model conversion - ENABLED for M3 Ultra (macOS-only)
cryptography>=41.0.0
hypothesis>=6.91.0  # Property-based testing
datasets>=2.14.0
# Pin datasets subdependencies to prevent backtracking
diffusers>=0.35.0  # Constrain to latest stable to avoid backtracking
etcd3>=0.12.0  # etcd client for consensus (provides 'etcd3' module)
fastapi>=0.104.0
fastmcp==2.1.0  # Pinned to avoid extensive backtracking; compatible with mcp 1.6.0+
# flash-attn moved to requirements-optional.txt (requires torch pre-installed for build)
# genesis-world==0.3.3  # DISABLED: 3GB+ deps (mujoco, vtk, torch) exceed CI disk. In requirements-optional.txt
httpx>=0.28.1  # Compatible with google-genai and pytest-httpx
huggingface_hub>=0.16.0
isort>=5.10.0
librosa>=0.10.0  # For audio feature extraction
msgpack>=1.0.0
mypy>=1.0.0
myst-parser>=2.0.0
# Adopt NumPy 2 to satisfy OpenCV >=4.12 and modern deps
numpy>=2.1,<2.3  # Matches OpenCV 4.12 constraint (<2.3)
opencv-python-headless==4.12.0.88  # Requires numpy >=2,<2.3
opentelemetry-api==1.23.0  # Security-pinned; aligns with otel 1.23 + instrumentation 0.44b0
orjson>=3.10.0
outlines>=0.0.37  # For constrained generation with LLMs
peft>=0.18.0  # Required for transformers 4.46+, Qwen3 support
Pillow>=10.0.0
playwright>=1.46.0
pre-commit>=3.0.0
pretty_midi>=0.2.10  # For orchestral MIDI parsing (symphony renderer)
prometheus-client>=0.23.1,<1.0.0
psutil>=5.9.0
python-jose[cryptography]>=3.3.0  # JWT handling for auth

# HAL Hardware Adapters — moved to requirements-optional.txt
# pyaudio requires PortAudio system library (macOS: brew install portaudio)
pydantic>=2.7.1,<3.0.0  # Compatible with genesis-world and mcp requirements
pytest-asyncio>=0.20.0
pytest-cov>=4.0.0
pytest-html>=4.0.0
pytest-httpx>=0.27.0  # Compatible with httpx 0.27.*
pytest-json-report>=1.5.0
pytest-playwright>=0.5.2
pytest-timeout>=2.2.0
pytest-xdist>=3.0.0
pytest>=7.0.0
python-dotenv>=1.0.0
python-socketio>=5.11.0  # Socket.IO server
PyYAML>=6.0.0
redis>=4.0.0
ruff>=0.1.0
safetensors>=0.3.0
safety==3.2.5  # Compatible with pydantic 2.7.1
scikit-learn>=1.3.0
scipy>=1.10.0
sentence-transformers>=2.2.0  # For CLIP embeddings
sounddevice>=0.4.6  # For real-time audio I/O (spatial audio, microphones)
soundfile>=0.12.0  # For audio file I/O
sphinx-rtd-theme>=1.3.0
sphinx>=7.0.0
sqlalchemy>=2.0.0
tenacity>=8.2.0
torch>=2.6.0  # Updated for CVE-2025-32434 security fix
transformers>=4.46.0  # Updated for Qwen3 support and PEFT compatibility
sentencepiece>=0.2.0  # Required for WildGuard safety classifier tokenizer
trimesh>=4.0.0
twine>=4.0.0
types-aiofiles>=24.1.0
types-bleach>=6.1.0
types-cachetools>=5.0.0  # Q1 2025 type safety campaign
types-croniter>=6.0.0
types-PyYAML>=6.0.0  # Q1 2025 type safety campaign
types-redis>=4.6.0
uvicorn>=0.24.0
z3-solver>=4.12.0  # Formal Verification & Theorem Proving

# Video understanding models (temporal visual reasoning, physics priors)
# decord temporarily disabled for local dev on macOS arm64; use av instead
# decord>=0.6.0; platform_system!="Windows" and platform_machine!="aarch64"

# EMERGENCE FEATURES (December 2024)
gymnasium>=1.0.0

# LLM Enhancement Libraries
instructor>=1.0.0  # Structured outputs from LLMs

# Elysia RAG Integration (December 2025)

# Video/Audio Processing (January 2025)
# Note: whisperx requires torch~=2.8.0 and specific torchaudio version
# Install separately: pip install whisperx
# pyannote.audio>=3.4.0  # DISABLED: Pulls onnxsim which fails on macOS ARM64 CI runners
# sam2>=1.0.0  # Segment Anything Model 2 - DISABLED: requires 3GB+ torch deps, breaks CI disk space
pyicloud>=2.2.0  # Apple FindMy integration
aiosqlite>=0.19.0  # Async SQLite for testing
