# Production-Ready Framework Dependencies
# Required for comprehensive error handling, validation, logging, configuration, and resource management

# Configuration Management
pyyaml>=6.0               # YAML configuration file support
watchdog>=3.0             # File system monitoring for hot-reloading

# TOML Support (Python < 3.11)
tomli>=2.0; python_version < "3.11"

# System Monitoring and Resource Management
psutil>=5.9.0             # System and process monitoring

# Optional but recommended for enhanced functionality
uvloop>=0.17.0; sys_platform != "win32"  # High-performance async event loop

# Security and validation (if not already in main requirements)
cryptography>=40.0        # For secure token generation