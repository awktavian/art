<?xml version="1.0" encoding="utf-8"?>
<!--
    Kagami XR - AndroidXR Spatial Smart Home Interface

    Colony: Nexus (e4) - Integration

    h(x) >= 0. Always.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- XR Permissions -->
    <uses-permission android:name="android.permission.SCENE_UNDERSTANDING" />
    <uses-permission android:name="android.permission.HAND_TRACKING" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Audio for voice commands -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- XR Hardware Features -->
    <uses-feature android:name="android.hardware.xr.headset" android:required="true" />
    <uses-feature android:name="android.software.xr.immersive" android:required="false" />

    <!-- MinSdk override for XR libraries if needed -->
    <uses-sdk tools:overrideLibrary="androidx.xr.scenecore, androidx.xr.compose, androidx.xr.runtime, androidx.xr.arcore" />

    <application
        android:name=".KagamiXRApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KagamiXR"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|colorMode|density"
            android:theme="@style/Theme.KagamiXR">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- XR App category for AndroidXR launcher -->
                <category android:name="android.intent.category.XR_APP" />
            </intent-filter>
        </activity>

    </application>

</manifest>
