"""Scripts package for K os utilities.

Organized into subpackages: analysis/, benchmarks/, ci/, core/,
dev/, config/monitoring/, ops/, setup/, testing/, training/, validation/.
Most scripts are intended as CLI entry points, not imports.

Public exports: None (invoke scripts directly via CLI)
"""
