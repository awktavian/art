# Minimal CI dependencies for lint/typecheck/reporting.
#
# Intentionally does NOT include requirements.txt to avoid heavy ML/GPU installs
# when running lightweight checks (ruff/mypy) in CI.

ruff>=0.1.0
mypy>=1.0.0

pytest>=7.0.0
pytest-asyncio>=0.20.0
pytest-timeout>=2.2.0
pytest-xdist>=3.0.0
pytest-cov>=4.0.0

# Security scanning tools
bandit>=1.8.0
safety>=3.2.0
pip-audit>=2.6.0
detect-secrets>=1.4.0
cyclonedx-bom>=4.0.0

# Required by kagami type hints and API schemas
pydantic>=2.7.1,<3.0.0
